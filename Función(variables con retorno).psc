Funcion area <- CalculateAreaTriangulo(base, altitude)
    area <- (base * altitude) / 2
FinFuncion

Algoritmo main
    // We call the function and 
	//we store the result in a variable
    area <- CalculateAreaTriangulo(5, 8)
    Escribir "The area of the triangule is: ", area
FinAlgoritmo